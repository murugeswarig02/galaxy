name: Folder Workflow

on:
  push:
    paths:
      - 'src/'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run script
      run: echo "Triggered for src folder"
